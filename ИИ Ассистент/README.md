# Модуль ИИ ассистент бот

Telegram бот-консультант по профессиональной косметике с интеграцией RAG-системы.

## Описание

Модуль `ИИ Ассистент` представляет собой Telegram бота, который помогает консультировать клиентов по профессиональной косметике брендов Реведерм/Revederm, Себорадин/Seboradin, Доктор Шпиллер/Dr.Spiller. Бот использует RAG-систему для поиска релевантной информации о продуктах в векторной базе данных.

## Возможности

- 🤖 Консультации по косметике с использованием RAG-системы
- 💬 Интеграция с OpenAI через ProxyApi
- 📊 Сохранение всех диалогов в Google Sheets
- 🔍 Поиск в векторной базе данных Chroma DB
- 🔘 Закрепленные кнопки для быстрого доступа к функциям
- 📞 Сбор данных для консультаций (имя, телефон)
- 🛒 Сбор данных для заказов (имя, телефон, детали заказа)
- 📢 Автоматическая отправка заявок в группу Telegram

## Структура модуля

```
ИИ Ассистент/
├── main.py                 # Основной файл запуска
├── telegram_bot.py         # Модуль Telegram бота
├── rag_service.py          # Сервис для работы с RAG (Chroma DB)
├── openai_service.py       # Сервис для работы с OpenAI через ProxyApi
├── sheets_service.py       # Сервис для работы с Google Sheets
├── config.py               # Конфигурация приложения
├── requirements.txt        # Зависимости Python
├── README.md               # Эта документация
├── SETUP.md                # Инструкция по настройке
├── DOCKER_DEPLOY.md        # Инструкция по развертыванию в Docker
├── GOOGLE_SHEETS_SETUP.md  # Инструкция по настройке Google Sheets
├── start_bot.bat           # Скрипт запуска бота (Windows)
├── stop_bot.bat            # Скрипт остановки бота (Windows)
├── Dockerfile              # Docker образ
└── docker-compose.yml      # Docker Compose конфигурация
```

## Установка и настройка

### 1. Установка зависимостей

```bash
cd "ИИ Ассистент"
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (на уровень выше модуля) или в папке модуля:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_GROUP_ID=your_telegram_group_id_here

# OpenAI Proxy API
OPENAI_PROXY_API_URL=https://api.proxyapi.ru/openai/v1
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Google Sheets API
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here

# Chroma DB
CHROMA_DB_PATH=../chroma_db

# Embedding Model
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
```

### 3. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Добавьте токен в `.env` файл
4. Создайте группу в Telegram для получения заявок
5. Добавьте бота в группу как администратора
6. Получите ID группы (отрицательное число, например: -1001234567890)
   - Можно использовать бота [@userinfobot](https://t.me/userinfobot) в группе
   - Или через API: `https://api.telegram.org/bot<TOKEN>/getUpdates`
7. Добавьте `TELEGRAM_GROUP_ID` в `.env` файл

### 4. Настройка Google Sheets API

Подробная инструкция находится в файле `GOOGLE_SHEETS_SETUP.md`.

Кратко:
1. Создайте проект в Google Cloud Console
2. Включите Google Sheets API и Google Drive API
3. Создайте сервисный аккаунт и скачайте JSON ключ
4. Переименуйте файл в `credentials.json` и поместите в корень проекта
5. Создайте Google таблицу и поделитесь ею с email сервисного аккаунта
6. Добавьте `GOOGLE_SHEETS_SPREADSHEET_ID` в `.env` файл

### 5. Подготовка векторной базы данных

Перед запуском бота необходимо создать векторную базу данных. Используйте модуль `Создание векторной базы`:

```bash
cd "../Создание векторной базы"
python build_rag.py
```

База данных будет создана в папке `../chroma_db/`.

## Запуск приложения

### Основной запуск

```bash
cd "ИИ Ассистент"
python main.py
```

Или напрямую:

```bash
python telegram_bot.py
```

### Запуск через скрипт (Windows)

```bash
start_bot.bat
```

### Остановка бота (Windows)

```bash
stop_bot.bat
```

### Запуск в Docker

Подробная инструкция находится в файле `DOCKER_DEPLOY.md`.

Кратко:
```bash
docker-compose up -d
```

## Тестирование модулей

### Тест RAG сервиса

```bash
python rag_service.py
```

### Тест OpenAI сервиса

```bash
python openai_service.py
```

### Тест Google Sheets сервиса

```bash
python sheets_service.py
```

## Работа с ботом

1. Найдите вашего бота в Telegram и отправьте команду `/start`
2. Бот поприветствует вас и предложит ответить на вопросы
3. Внизу экрана всегда видны две кнопки:
   - **"Связаться с менеджером"** - для консультации (запросит имя и телефон)
   - **"Сделать заказ"** - для оформления заказа (запросит имя, телефон и детали заказа)
4. Задавайте вопросы о косметике в текстовом режиме
5. Все диалоги автоматически сохраняются в Google Sheets
6. Заявки автоматически отправляются в указанную группу Telegram

## Особенности работы

- Бот использует RAG-систему для поиска релевантной информации о продуктах
- Все ответы формируются на основе данных из векторной базы
- История диалога сохраняется для каждого пользователя
- **Все диалоги** (включая нажатия кнопок) сохраняются в Google Sheets (лист "Диалоги")
- Заявки на консультации сохраняются в Google Sheets (лист "Консультации")
- Заявки на заказы сохраняются в Google Sheets (лист "Заказы")
- Все заявки автоматически отправляются в указанную группу Telegram
- Кнопки закреплены и всегда видны внизу экрана
- Бот не повторяет приветствие после первого сообщения

## Системный промпт

Бот работает как Алекс - эмпатичный консультант-косметолог, который:
- Задает уточняющие вопросы для понимания потребностей
- Предлагает подходящие продукты на основе запросов
- Предоставляет полную информацию о продуктах
- Предлагает альтернативные варианты

## Устранение неполадок

### Бот не отвечает

- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Убедитесь, что бот запущен (`python main.py`)
- Проверьте логи в папке `logs/`

### Ошибки при работе с RAG

- Проверьте, что папка `../chroma_db/` существует и содержит данные
- Убедитесь, что модель эмбеддингов загружается корректно
- Проверьте путь к базе данных в `.env` файле

### Ошибки Google Sheets

- Проверьте наличие файла `credentials.json`
- Убедитесь, что сервисный аккаунт имеет доступ к таблице
- Проверьте правильность `GOOGLE_SHEETS_SPREADSHEET_ID`

### Ошибки OpenAI API

- Проверьте правильность `OPENAI_API_KEY`
- Убедитесь, что прокси-сервер доступен
- Проверьте баланс API ключа

### Ошибки отправки в группу

- Проверьте, что `TELEGRAM_GROUP_ID` установлен в `.env` файле
- Убедитесь, что бот добавлен в группу как администратор
- Проверьте, что ID группы правильный (отрицательное число)
- Убедитесь, что бот имеет права на отправку сообщений в группу

## Зависимости

- `python-telegram-bot>=22.5` - Telegram Bot API
- `openai>=2.15.0` - OpenAI API клиент
- `chromadb>=1.4.1` - Векторная база данных
- `sentence-transformers==2.3.1` - Модели эмбеддингов
- `gspread==5.12.0` - Google Sheets API
- `google-api-python-client==2.108.0` - Google API клиент
- `google-auth-httplib2==0.1.1` - Google Auth
- `google-auth-oauthlib==1.2.0` - Google OAuth
- `python-dotenv==1.0.0` - Загрузка переменных окружения
- `requests==2.31.0` - HTTP библиотека
- `tqdm>=4.65.0` - Прогресс-бар

## Дополнительная документация

- `SETUP.md` - Подробная инструкция по настройке
- `DOCKER_DEPLOY.md` - Развертывание в Docker
- `GOOGLE_SHEETS_SETUP.md` - Настройка Google Sheets API
